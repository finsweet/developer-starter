name: CI

on:
  pull_request:

jobs:
  Lint:
    uses: finsweet/workflow-ci-lint/.github/workflows/lint.yml@2

  # Comment this job out if your project doesn't have any tests
  Tests:
    uses: finsweet/workflow-ci-test/.github/workflows/test.yml@2
