name: Release

on:
  push:
    branches:
      - master
      - main

jobs:
  release:
    uses: finsweet/workflow-release/.github/workflows/release.yml@2
    secrets:
      NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
